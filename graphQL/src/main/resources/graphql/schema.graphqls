type Query {
  newPuzzle(difficulty: Difficulty): Sudoku
}

type Sudoku {
  cells: [Cell!]!
  solution: Sudoku
  difficulty: Difficulty!
}

enum Difficulty {
  RANDOM,
  EASY,
  MEDIUM
}

type Cell {
  row: Int!
  column: Int!
  value: Int
}